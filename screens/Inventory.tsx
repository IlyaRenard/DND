import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Inventory = () => {
  return (
    <View>
      <Text className="text-4xl text-white">Inventory</Text>
    </View>
  );
};

export default Inventory;

const styles = StyleSheet.create({});
